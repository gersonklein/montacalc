{
  "name": "montacalc",
  "version": "1.0.0",
  "description": "Plataforma educacional de blocos para reconstruir a calculadora de referência (Bloco -> IR -> JS -> RUNTIME).",
  "private": true,
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "blockly": "^13.2.1",
    "jsdom": "^29.0.0"
  }
}
